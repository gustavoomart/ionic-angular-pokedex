import { KebabToTitlePipe } from "./kebab-to-title";

describe('KebabToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new KebabToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
